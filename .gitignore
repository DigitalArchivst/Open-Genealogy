# Claude Code local files
.claude/
CLAUDE.md
/.agents/
/AGENTS.md

# Credentials and local environment files
.env
.env.*
*.pem
*.key

# Binary documents (use markdown versions instead)
*.pdf
*.docx

# Exception: companion PDFs that are deliberate deliverables
!image-analysis/Deep-Look-v2-Four-Models-Full-Results.pdf

# Internal planning (not for public repo)
open-genealogy-gap-analysis.md
open-genealogy-pdr.md
notes/

# Test results (contain full model responses, timestamps)
skills/gra/tests/results/
__pycache__/

# Local .txt conversions of references for Custom GPT Knowledge uploads
skills/gra/references/*.txt

# Release artifacts (published via gh release, not committed)
*.zip
dist/
